export class User {
  name: string;
  isTrue: Boolean;
  someNumber: number;

  constructor(name: string, isTrue: Boolean, someNumber: number){
    this.name = name;
    this.isTrue = isTrue;
    this.someNumber = someNumber;
  }
}
